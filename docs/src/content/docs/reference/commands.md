---
title: Commands and shortcuts
description: Every slash command and keyboard shortcut.
---

:::caution[Placeholder]
This page is a stub. Content is planned but not written yet.
:::

## Planned content

Generated from `CommandSpecs` / `helpText` so it cannot drift.

- Command groups: Getting started, Model, Session, Trust, UI, Maintenance
- Per command: usage, description, and behavior notes
  (`/plan`, `/act`, `/help`, `/model`, `/models refresh`, `/thinking`,
  `/provider`, `/web`, `/session`, `/stats`, `/new`, `/resume`, `/fork`,
  `/copy`, `/name`, `/compact`, `/yolo`, `/permissions`, `/theme`, `/doctor`,
  `/reload`, `/quit`, `/exit`)
- Argument validation and the exact usage errors
- Composer completion behavior, including model, session, theme, provider, and
  fork suggestions
- Skill (`/skill:<name>`) and prompt (`/name`) dispatch, and the ordering rules
  between built-ins, prompts, skills, and extension commands
- Keyboard shortcuts live on the [Keyboard shortcuts](/reference/keybindings/)
  page
